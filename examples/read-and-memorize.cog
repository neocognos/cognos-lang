# Read a file and extract key facts
flow summarize(path: String):
    content = read(file(path))
    summary = think(content, system="Summarize in 3 bullet points.")
    emit(summary)

flow main():
    path = read(prompt="File path")
    summarize(path)
