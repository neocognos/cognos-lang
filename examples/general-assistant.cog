# General-purpose agentic assistant
# The runtime binds `input` from whatever channel invokes this flow.

flow general_assistant(input: String):
    response = input
    loop max=30:
        response = think(response)
        if response.has_tool_calls:
            result = act(response)
            response = result
        else:
            break
    emit(response)
