[package]
name = "cognos"
version = "0.1.0"
edition = "2021"
description = "Cognos — an agentic programming language"

[dependencies]
anyhow = "1"
reqwest = { version = "0.12", features = ["blocking", "json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
